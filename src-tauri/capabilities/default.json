{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for both windows.",
  "windows": ["settings", "overlay"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:webview:default",
    "core:window:default",
    "store:default",
    "shell:default",
    "global-shortcut:default"
  ]
}
